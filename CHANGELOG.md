# Changelog

## v0.1.0

- Initial public MVP release.
- Self-documenting CLI (`dt list`, `dt tags`, `dt help <tool>`).
- Included tools:
  - `ai-context` (scan/update shareable AI context)
  - `zip-project` (share-friendly project zips)
  - `git-aliases`, `jump-dir`, `prompt`
- Bats test suite under `test/`.
