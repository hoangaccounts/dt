# `dt docs`

This directory contains long-form design and system documentation for the `dt docs` tool.

## Suggested shape

- `00-foundation/` for tool purpose, CLI contract, architecture, and canonical specs
- `10-product-systems/` if the tool grows into multiple subsystems
- `30-delivery/` for rollout or migration notes
- `40-decisions/` for tool-specific ADRs
